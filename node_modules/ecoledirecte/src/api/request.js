module.exports = function(url, method) {
    const axios = require('axios').default;
    return function(datas, params) {
        return axios[method](url, `data=${JSON.stringify(datas)}`, {params: params});
    }
}